# ── Site ──────────────────────────────────────────────────────────────────────
# Used for SEO: metadataBase, sitemap, canonical URLs, OG tags
NEXT_PUBLIC_SITE_URL=https://truematchadvisory.com

# ── Resend (email) ───────────────────────────────────────────────────────────
# Get your API key at https://resend.com
RESEND_API_KEY=re_xxxxxxxxxxxx

# Mail addresses
MAIL_FROM=kontakt@truematchadvisory.com
MAIL_REPLY_TO=natalia@truematchadvisory.com
MAIL_TO=natalia@truematchadvisory.com

# ── Upstash Redis (rate limiting — optional) ─────────────────────────────────
# Required for distributed rate limiting in multi-instance deployments.
# Without these, the app uses an in-memory rate limiter (works for single-instance Vercel).
# UPSTASH_REDIS_REST_URL=https://xxxxxxxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=AXxxxxxxxxxxxxxx

# ── Cloudflare R2 (CV storage — optional, future) ───────────────────────────
# For storing candidate CVs externally instead of email attachments.
# Not currently used — prepared for future integration.
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=truematch-cv
# R2_PUBLIC_BASE_URL=https://cv.truematchadvisory.com
